version: "3.8"
services:
  glassbox-validator:
    build: .
    ports:
      - "8000:8000"
    environment:
      - ENV=production
    volumes:
      - ./cong:/app/cong
    restart: unless-stopped
